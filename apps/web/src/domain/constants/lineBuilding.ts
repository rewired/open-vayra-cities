/**
 * Minimum number of ordered stops required before a build-line draft can be completed.
 */
export const MINIMUM_STOPS_REQUIRED_TO_COMPLETE_LINE = 2;

/**
 * Deterministic label prefix used for locally generated placeholder line labels.
 */
export const LINE_BUILD_PLACEHOLDER_LABEL_PREFIX = 'Line';
