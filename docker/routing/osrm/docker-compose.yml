version: "3.8"

services:
  osrm:
    image: osrm/osrm-backend
    container_name: city-ops-osrm
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ../../../data/routing/osrm:/data
    command: "osrm-routed --algorithm mld /data/hamburg-latest.osrm"
